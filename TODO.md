# TODOs and sample issues

These items are here so the RAG pipeline can answer questions like "What TODOs exist?" or "What issues are open?".

1. **Add input validation to `math.js`** — Functions like `add` and `multiply` do not validate types; consider rejecting non-numbers (e.g. strings, null) or handling NaN.
2. **Support optional locale in `greet.js`** — Allow localized greetings (e.g. "Hola", "Bonjour") via a locale option or external strings.
